# Frontend build-time env (Vite). Copy to .env and fill in.
# NOTE: .env is tracked in git for this repo and OVERRIDES platform (Cloudflare/Lovable)
# env vars at build time — see PROJECT_CONTEXT.md §3.7. The frontend reads the
# PUBLISHABLE key, not the ANON key.
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""
VITE_RAZORPAY_KEY_ID=
